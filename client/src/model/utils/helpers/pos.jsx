/**  pos
 *  checks if a value is positive or negative and only returns it if it is positive, if not it will return 0.
 *@param number number of arguments that will be passed into the first function
 *@returns the number or 0
 **/

export const pos = (number) => (number > 0 ? number : 0);
